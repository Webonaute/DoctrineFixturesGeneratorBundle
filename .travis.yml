language: php

php:
    - 7.0
    - 7.1
    - 7.2
    - 7.3

env:
  - SYMFONY_VERSION=2.2.*
  - SYMFONY_VERSION=2.3.*
  - SYMFONY_VERSION=2.7.*
  - SYMFONY_VERSION=dev-master

before_script: composer install --dev --prefer-source
